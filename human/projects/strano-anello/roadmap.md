# Roadmap

Now:
- Monitorare i prossimi run: `watcher_comment_diagnostics` deve restare vicino a 102 righe.

Next:
- Valutare unit systemd opzionale per `tools/strano_db_maintenance.py anomalies`.
- Aggiungere CI GitHub se il repo deve validare test a ogni push.

Later:
- Valutare rimozione tabelle legacy `sa_*` solo con revisione esplicita.
