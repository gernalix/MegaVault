# Windows Flight Recorder Features

features=SQLite WAL event history, EventLog ingestion, periodic metrics, heartbeat Kuma, watchdog, backups, CLI queries, live watch, log tail, doctor checks.
scope=Windows 11 local machine.
limits=admin-only logs and sensors depend on system permissions/drivers.
