# Windows Flight Recorder Features

features=SQLite WAL event history, EventLog ingestion, periodic metrics, fast USB/mount polling, session lock polling, heartbeat Kuma, watchdog, backups, CLI queries, probe diagnostics, live watch/debug, log tail, doctor checks, local dashboard/API/export.
scope=Windows 11 local machine.
limits=admin-only logs and sensors depend on system permissions/drivers.
