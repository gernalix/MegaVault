# Windows Flight Recorder Roadmap

now=runtime hardened; dashboard/API/probe/watch debug implemented; Startup fallback active.
next=verify admin-created Scheduled Tasks from elevated shell with doctor.
later=optional Windows service wrapper if Task Scheduler policy blocks SYSTEM tasks.
