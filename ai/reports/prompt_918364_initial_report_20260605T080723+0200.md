# Prompt 918364 - report iniziale

Data: 2026-06-05 08:07 CEST / 06:07 UTC

## Scope

- Mint host: `codex-token-watcher` / `codex-usage-monitor`.
- Oracle VM: `oracle-backup`, remote quota OCI, root filesystem `/`.
- Policy: niente bypass Cloudflare; niente cancellazione remota/restic/DB/WAL/log o dati utente senza verifica.

## Codex token watcher

- Timer: `codex-usage-monitor.timer` loaded/enabled/active waiting, prossimo run 2026-06-05 09:00 CEST.
- Service: `codex-usage-monitor.service` static, inactive/dead dopo ultimo run.
- DB: `/home/daniele/.local/share/codex-usage-monitor/codex_usage.sqlite3`.
- Ultimo run: id `71`, `2026-06-05T06:00:01Z`, `status=error`, `kuma=down:ok`.
- Ultimo errore: `dashboard blocked by Cloudflare/challenge; persistent Chrome profile needs manual verification`.
- Diagnostica ultima: `/home/daniele/.local/state/codex-usage-monitor/diagnostics/20260605T060053Z-dashboard-parse.{txt,html,png}`.
- Profilo Chrome persistente: `/home/daniele/.local/share/codex-usage-monitor/chrome-profile/Default`.
- `doctor`: Chrome OK, cookie presenti per domini OpenAI/ChatGPT, ambiente grafico OK, pagina finale `Just a moment...`.
- Causa probabile: challenge Cloudflare nel profilo corretto; non e' un problema di timer, DB o mapping Kuma.
- Azione ammessa: flow manuale `CODEX_USAGE_HEADLESS=0 ./codex_usage_monitor.py open-login`; nessun bypass.

## Oracle backup / quota remota

- Host: `instance-20260201-1126`.
- `/`: `/dev/sda1`, 45G totali, 39G usati, 6.6G liberi, 86%.
- Timer attivi:
  - `oracle-backup.timer` ogni 30 minuti.
  - `oracle-backup-monitor.timer` ogni 5 minuti.
  - `oracle-backup-healthcheck.timer` ogni 10 minuti.
  - `oracle-backup-remote-quota.timer` ogni ora.
  - `oracle-backup-localprune.timer`.
  - `oracle-backup-prune.timer` giornaliero.
- Ultimo backup osservato: service `oracle-backup.service` success, stato applicativo `REMOTE_DEGRADED`.
- Remote repo: `rclone:oci:bucket-20260206-0730/oraclevm`.
- Remote quota path: `oci:bucket-20260206-0730`.
- Remote quota: 7474 objects, 22.262 GBytes / 23903533548 bytes, soglia critical 21 GiB, assumed limit 22 GiB.
- Stato backup: `StorageLimitExceeded during write preflight`; remoto saltato per cooldown, fallback locale valido.
- Rate-limit gia' attivo:
  - `BACKUP_MONITOR_WARNING_REMINDER_SECONDS=86400`.
  - `BACKUP_MONITOR_CRITICAL_REMINDER_SECONDS=3600`.
  - `HEALTHCHECK_WARNING_REMINDER_SECONDS=86400`.
  - `HEALTHCHECK_CRITICAL_REMINDER_SECONDS=21600`.
  - `REMOTE_PREFLIGHT_FAILURE_COOLDOWN_SECONDS=21600`.
- Repo remoto read-only senza lock:
  - 837 snapshot.
  - gruppo unico `host=instance-20260201-1126 tags=oracle-vm,autosnap-5min`.
  - oldest `2026-05-02T01:33:03Z`, newest `2026-05-05T05:27:01Z`.
- Policy retention configurata:
  - `RESTIC_KEEP_LAST=48`.
  - `RESTIC_FORGET_GROUP_BY=host,tags`.
  - script `/opt/oracle_backup/prune.sh` esegue `restic forget --tag oracle-vm --keep-last "$RESTIC_KEEP_LAST" --group-by "$RESTIC_FORGET_GROUP_BY"` e `restic prune`.
- Criticita': il backend remoto pieno non permette neppure la creazione del lock restic; `restic snapshots` e `restic forget --dry-run` standard falliscono su `StorageLimitExceeded` nel path `locks/...`.
- Decisione iniziale: non eseguire `forget/prune` distruttivo fuori dallo script/policy finche' il lock non e' ottenibile; proporre sblocco remoto con quota temporanea o finestra manuale esplicita se necessario.

## Oracle fallback locale

- Local fallback repo: `/var/lib/oracle_backup/emergency_repo`.
- Snapshot fallback: 27.
- Oldest `2026-06-03T14:10:44Z`, newest `2026-06-05T05:11:33Z`.
- Stats fallback: 9.572 GiB raw-data.
- Stato healthcheck: fallback locale valido, emergency repo 10.1 GiB sotto soglia 20 GiB.

## Oracle root usage

Top-level:

- `/var`: 23G.
- `/home`: 8.8G.
- `/usr`: 3.8G.
- `/root`: 931M.
- `/opt`: 255M.

Backup-related:

- `/var/lib/oracle_backup`: principale contributore dentro `/var`.
- `emergency_repo`: circa 10.1 GiB, valido, non toccare.
- `sqlite_snapshots`: circa 4.7 GiB, retention count=1, valido, non toccare.

Log/cache:

- Journald: 584M.
- `/var/cache/apt`: 130M, `/var/cache/apt/archives`: 28K.
- `/tmp`: 720K, `/var/tmp`: 32K.
- `/var/log/oracle_backup`: 95M.
- `/var/log/syslog.1`: 571007086 bytes, rotated 2026-05-31.

## Candidati cleanup sicuri

1. `sudo apt-get clean`
   - Motivo: cache apt ricreabile; non contiene dati utente o backup.
2. `sudo journalctl --vacuum-size=300M`
   - Motivo: riduce solo journal archiviati mantenendo journald operativo e log recenti.
3. `sudo gzip -9 /var/log/syslog.1`
   - Motivo: log ruotato molto grande; compressione preserva contenuto diagnostico invece di eliminarlo.

Non candidati:

- `/var/lib/oracle_backup/emergency_repo`.
- `/var/lib/oracle_backup/sqlite_snapshots`.
- DB in `/home/ubuntu/db` e `/home/ubuntu/sync_root/db`.
- Repo restic remoto.
- WAL/SHM/DB/log recenti correnti.
